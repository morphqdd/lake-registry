## Publish summary

- Package name: `<name>`
- Version: `<version>`
- Git URL: `<https://...>`
- Ref (tag/branch/SHA): `<v0.1.0 | master | 40-char-sha>`

## Checklist

- [ ] `packages/<first-letter>/<name>.lake` exists / new version line appended
- [ ] `package "<name>"` matches the filename
- [ ] No duplicate `version "<v>"` line
- [ ] `rev` is a stable ref (tag preferred, branch acceptable, SHA only if frozen)
- [ ] Linked issue describes the package + use case (for first-time submissions)

## Linked issue

Closes #<num>
